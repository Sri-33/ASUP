import pandas as pd
df = pd.read_csv("sales.csv")
print("Summary:\n", df.describe())
print("Total sales by region:\n", df.groupby("Region")["Sales"].sum())
print("Top salesperson:\n", df.groupby("Salesperson")["Sales"].sum().idxmax())
