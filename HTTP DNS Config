enable
configure terminal
hostname R1

interface g0/0
 ip address 192.168.10.1 255.255.255.0
 no shutdown
exit

! (Optional: prevent DNS lookup delays)
no ip domain-lookup

end
write memory     ! or "wr"
